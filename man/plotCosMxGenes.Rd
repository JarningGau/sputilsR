% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cosmx.R
\name{plotCosMxGenes}
\alias{plotCosMxGenes}
\title{Plot spatial expression of multiple genes in CosMx data}
\usage{
plotCosMxGenes(
  polygons,
  tx,
  genes,
  colors = NULL,
  zoom.x = NULL,
  zoom.y = NULL,
  zoom.width.x = 1000,
  zoom.width.y = 1000,
  microns_per_pixel = 0.12028,
  scale_length_um_full = 50,
  scale_length_um_zoom = 25,
  label_size_full = 22,
  label_size_zoom = 25,
  point_size_full = 0.3,
  point_size_zoom = 2,
  polygon_linewidth_full = 0.3,
  polygon_linewidth_zoom = 1,
  show_zoom_rect = TRUE,
  zoom.rect.color = "white",
  zoom.rect.alpha = 0,
  zoom.rect.linewidth = 1,
  zoom.rect.linetype = "dashed"
)
}
\arguments{
\item{polygons}{A data frame containing cell polygon coordinates. Must include
columns: \code{x_global_px}, \code{y_global_px}, \code{fov}, and \code{cellID}.}

\item{tx}{A data frame containing transcript location data. Must include
columns: \code{x_global_px}, \code{y_global_px}, and \code{target} (gene name).}

\item{genes}{A character vector of gene names to plot. Each gene will be
displayed in a different color.}

\item{colors}{Optional character vector of colors for each gene. If \code{NULL},
colors are automatically generated using \code{rainbow()}. Must match the
length of \code{genes}.}

\item{zoom.x}{Optional numeric value in \link{0, 1} specifying the relative x position
of the zoom window bottom-left corner (0 = left, 1 = right, 0.5 = center). Default: 0.5.}

\item{zoom.y}{Optional numeric value in \link{0, 1} specifying the relative y position
of the zoom window bottom-left corner (0 = top, 1 = bottom, 0.5 = center). Default: 0.5.}

\item{zoom.width.x}{Numeric. Width of the zoom window in pixels (x-direction).
Default: 1000.}

\item{zoom.width.y}{Numeric. Height of the zoom window in pixels (y-direction).
Default: 1000.}

\item{microns_per_pixel}{Numeric. Conversion factor from pixels to microns.
Default: 0.12028.}

\item{scale_length_um_full}{Numeric. Length of the scale bar in microns for
the full view. Default: 50.}

\item{scale_length_um_zoom}{Numeric. Length of the scale bar in microns for
the zoom view. Default: 25.}

\item{label_size_full}{Numeric. Font size for the scale bar label in the full
view. Default: 22.}

\item{label_size_zoom}{Numeric. Font size for the scale bar label in the zoom
view. Default: 25.}

\item{point_size_full}{Numeric. Size of gene expression points in the full view.
Default: 0.3.}

\item{point_size_zoom}{Numeric. Size of gene expression points in the zoom view.
Default: 2.}

\item{polygon_linewidth_full}{Numeric. Line width for cell polygon borders in
the full view. Default: 0.3.}

\item{polygon_linewidth_zoom}{Numeric. Line width for cell polygon borders in
the zoom view. Default: 1.}

\item{show_zoom_rect}{Logical. Whether to display a rectangle on the full view
indicating the zoom region. Default: \code{TRUE}.}

\item{zoom.rect.color}{Character. Color of the zoom rectangle border.
Default: "white".}

\item{zoom.rect.alpha}{Numeric. Transparency of the zoom rectangle fill (0-1).
Default: 0 (fully transparent).}

\item{zoom.rect.linewidth}{Numeric. Line width of the zoom rectangle border.
Default: 1.}

\item{zoom.rect.linetype}{Character. Line type of the zoom rectangle border
(e.g., "solid", "dashed", "dotted"). Default: "dashed".}
}
\value{
A list containing two ggplot2 objects:
\itemize{
\item \code{full}: A ggplot2 object showing the full spatial view with all
genes plotted as colored points.
\item \code{zoom}: A ggplot2 object showing the zoomed-in view, or \code{NULL}
if zoom parameters are not provided.
}
}
\description{
Creates spatial transcriptomics visualizations for CosMx data, displaying
the expression of multiple genes as colored points overlaid on cell polygons.
The function generates both a full view of the entire dataset and an optional
zoomed-in view of a specified region.
}
\examples{
\dontrun{
# Example usage:
plots <- plotCosMxGenes(
    polygons = cell_polygons,
    tx = transcript_data,
    genes = c("Gene1", "Gene2", "Gene3"),
    colors = c("red", "green", "blue"),
    zoom.x = 0.5,
    zoom.y = 0.5
)

# Display the plots
plots$full
plots$zoom
}
}
